var studentName=['nafis', 'alvira','abir','mahamud'];
// Add last element use push
// add first element to use unshift


studentName.push('FAtin');
studentName.unshift('Fariha');

console.log(studentName);

// remove last element to use pop
// remove first element to use shift

studentName.shift();
console.log(studentName);
